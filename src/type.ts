//  Simple Type can be define 

type NameCard = string

type Product ={
    id:number,
    name:string,
    price:number
}

// unoin type
 
type UserRole = "Admin" | "User"  // secific value 

type PhoneNumber = string  | number

type Myarray = (string | number)[]









function Getproduct(value:Product):Product{
    return value

}

// 

